<% 
    formatSummary = (content, maxLength = 150) => { 
        return content.replace(/(<([^>]+)>)/igm,"").substr(0, maxLength);
    } 
%>
<% 
    formatSummary2 = (content, maxLength = 300) => { 
        return content.replace(/(<([^>]+)>)/igm,"").substr(0, maxLength);
    } 
%>






